name: Demo Workflow

on: workflow_dispatch

jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: Print to console
        run: echo Hello github Action
      - name: Print to new Lines
        run: |
          echo First line
          echo Second line
    
